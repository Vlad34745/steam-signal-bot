[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = [
    "E501",  # line length handled loosely via line-length above, not a hard failure
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
